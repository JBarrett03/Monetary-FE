import { Injectable } from '@angular/core';

/**
 * The AccountService is a placeholder service that is currently empty. It is decorated with the @Injectable decorator, which allows it to be injected into other components or services in the Angular application. The providedIn property is set to 'root', indicating that this service will be available as a singleton throughout the entire application.
 */
@Injectable({
  providedIn: 'root',
})

/**
 * The AccountService class is defined as an empty service that can be used for managing account-related functionality in the future. It is currently a placeholder and does not contain any methods or properties. This service can be expanded upon in the future to include logic for handling user accounts, authentication, or other related features as needed.
 */
export class AccountService {
  
}
